# Dinja Server Configuration
# Copy this file to .env and adjust the values as needed

# Server host (use 0.0.0.0 for Docker, 127.0.0.1 for local dev)
HOST=0.0.0.0

# Server port
PORT=8080

# Static files directory (contains engine.min.js, etc.)
STATIC_DIR=./core/static

# Rust log level (error, warn, info, debug, trace)
RUST_LOG=info

# Docker image configuration (for docker-build.sh)
IMAGE_NAME=dinja
IMAGE_TAG=latest
